# HILiT — 에이전트 작업 규칙

**제품** 찍어둔 긴 영상에서 AI가 사용자를 추적해 하이라이트를 찾아주고, **공개 여부와 무관하게 개인 기록으로 남기는** 숏폼 스포츠 영상 서비스 (농구 우선)

---

## 문서 우선순위

충돌 시 위가 이긴다.

| 순위 | 문서 | 역할 |
| :--: | --- | --- |
| 1 | `SRS/[SRS]hilit-SRSv1.8.md` | 🔴 **요구사항 단일 진실 원천** (REQ-FUNC 27 · REQ-NF 19) |
| 2 | `SRS/[SRS]hilit-SRSv2.0-nextjs.md` | 기술 제약 반영판 (Next.js·Supabase·Vercel) |
| 3 | `DS/[DS]hilit-DSv1.1.md` | 설계 명세 (API·엔티티·상태 기계) |
| 4 | `tasks/*.md` | 개발 태스크 54건 |
| 5 | `docs/grill/GRILL_LEDGER.md` | 미해소 결정 토픽 원장 |

---

## 🔴 절대 규칙 — 어길 경우 제품 정의가 무너진다

### 1. 기록이 공개보다 먼저다 (D4)

**공개 범위를 정하기 전에 기록이 이미 저장돼 있어야 한다.** 저장과 공개는 분리된 행위다.

- 기본 공개 범위는 **`private`** 이며 **DB `DEFAULT`** 로 강제한다 (ADR-4)
- 공개를 권유하는 문구·유도 UI를 만들지 않는다
- 공개 범위 화면을 *"게시하기"* 로 부르지 않는다

### 2. 사람이 고른다 (D2 · ADR-6 · ADR-7)

**사용자의 명시적 선택 없이 결과물이 확정되면 안 된다.**

**흐름 (ADR-6 — 완성본 선제안 + 병존)**

```
탐지 → AI 선택 → 리프레이밍 → 렌더 → 완성본 재생
                                      ├ [이대로 저장]
                                      └ [직접 고르기] → 후보 목록
```

- 🔴 **[직접 고르기] 경로를 없애지 않는다.** 없애면 D2가 이름만 남는다
- 🔴 **완성본 화면에 "AI가 무엇을 골랐는지" 표시**한다 — 없으면 판단 근거가 없다
- **미리 체크된 후보를 만들지 않는다.** 단 🔺 **[직접 고르기] 진입만 예외** — AI 선택분이 체크된 채 들어간다. 사용자가 그 경로를 명시적으로 골랐기 때문이다
- 사용자가 누르는 "전체 선택"은 허용 — **경계는 "누가 눌렀는가"** 이지 "몇 개인가"가 아니다

**기준 지정 (ADR-7 — 사용자가 입력)**

- 🔴 **시스템이 취향을 추론하지 않는다.** 사용자가 자연어로 기준을 입력한다
- 🔴 **Gemini를 인물 식별에 쓰지 않는다** — REF-15에서 인물 특정 근거는 **영상 자막**이었고 **아마추어 농구 원본에는 자막이 없다**
  - *"내가 나온 구간"* = 추적 API(D1) · *"무엇을 했는가"* = Gemini

### 3. 확신이 없으면 보여주지 않는다 (REQ-FUNC-027)

재식별 신뢰도가 임계 미만인 후보는 **목록에서 제외**한다.

- 🔴 **저신뢰 배지를 달아 목록에 남기지 않는다** — 판단을 사용자에게 떠넘기는 것이다
- 제외 건수를 함께 반환해 **"왜 후보가 적은지"** 를 알린다

### 4. 편집을 늘리지 않는다

§3 여정 분석: **④ 편집은 2차 이탈 지점**이다 (*"30분 초과 시 포기"*).

- 원본 자르기·회전, 후보 구간 직접 조정, 수동 구도 조정을 **추가하지 않는다**
- 🔴 *"한 번이라도 편집에서 지치면 다음 주에 삼각대를 아예 안 들고 간다"*

### 5. 팔로우는 권한이 아니다 (§6.3 규칙 8)

팔로우는 **공개 기록만** 정한다. **비공개 공유는 그룹이 맡는다.**

- 🔴 **RLS 정책에 팔로우 조건을 넣지 않는다**
- 타인 프로필에서 비공개 기록은 **개수에도 잡히지 않아야 한다** (SC-4.4)

### 6. 과금은 기능을 잠그지 않는다 (SRS ADR-8)

무료 사용자가 **업로드 → 탐지 → 후보 → 선택 → 리프레이밍 → 음악 → 렌더 → 저장 전 구간을 완주**한다.

**허용 3축** — 이 셋만 등급으로 나눈다

| 축 | 원가 근거 (REQ-NF-013) |
| --- | --- |
| 처리 분량 (월 편수 · 원본 분) | ① 추론 — 원본 분 비례 |
| 재작업 횟수 (같은 원본 재발행) | ③ 저장·전송 — 원본·탐지 산출물 보관 |
| 출력 규격 (해상도 · fps) | ③ 저장·전송 — 전송량 |

🔴 **금지 5건 — 등급 분기 조건으로 코드에 넣지 않는다**

| 금지 | 깨지는 것 |
| --- | --- |
| 탐지·재식별·리프레이밍 **품질 등급화** | **Gate A 판정이 무의미해진다** |
| 후보 **개수** 등급화 | Q7 실측 확정 불가 |
| **`[직접 고르기]` 접근 제한** | D2 · ADR-6 |
| 임계 **`τ`** 등급별 차등 | REQ-FUNC-027 |
| **기록 저장 · 공개 범위 3종** | D4 · Gate B — 유료 대상은 **원본·중간 산출물**이고 결과물 영구 보관은 무료다 |

- 🔴 **Gate A · Gate B는 무료 등급으로 판정한다.** 무료 등급도 탐지율 85% · 1080×1920 · `τ`를 **동일 값으로** 만족한다
- 🔴 **페이월을 첫 완주 이전에 노출하지 않는다** — Gate B 판정이 오염된다
- 🔴 **수작업 편집 도구를 유료로 붙이지 않는다** — 규칙 4는 유료라고 풀리지 않는다. 가장 비싼 코호트에게 이탈 지점을 파는 것이다
- 요금제 도입 시점은 **Gate B 통과 후**(PRD ADR-04). 지금 만드는 것은 **Entitlement Resolver Mock**(전부 무제한 반환)뿐이고, 🔴 **실제 교체 시 호출부 변경 0건**이어야 한다

---

## 숫자를 다루는 규칙

| 상황 | 표기 |
| --- | --- |
| 상위 문서에 있는 값 | `[SOURCE]` |
| 정성 → 정량 변환 | `[HYPOTHESIS]` 또는 `[PROPOSED]` |
| 기준선 없음 | **"Baseline: 미측정"** |
| 미정 | **"Target: TBD"** |

🔴 **지어내지 않는다** — 사용자 수·전환율·이탈률·정확도·지연·비용·경쟁사 성능·시장 점유율·인터뷰 표본 수·실험 결과.

### 🔺 확정값이 아닌 주요 숫자

| 값 | 상태 | 결정 조건 |
| --- | --- | --- |
| 후보 **30개** | `[SOURCE·초안]` A3 | Q7 — 15/30/50 실측 |
| 완성 영상 길이 | 🔴 **요구사항 없음** | T3 (grill 진행 중) |
| 음악 **500곡** 5갈래 | `[SOURCE·초안]` | Q16 · CC0 전환으로 재정의 필요 |
| 탐지 p95 **8분** | `[PROPOSED]` | 경쟁 기준 아님 |
| 렌더 p95 **90초** | `[PROPOSED]` | 단말 등급별 재정의 필요 |
| **무료 등급 쿼터** (월 편수 · 원본 분 · 재발행 · 보관 기간) | 🔴 **`[TBD]` 전부** | Q22 ← **Q3(편당 원가 상한) 선행** |
| 유료 전환율 | 🔴 **Baseline: 미측정** | 대조군은 CapCut 약 **0.44%**(매출 역산 `추정치`)뿐 — 자체 목표는 **Target: TBD** |

---

## 기술 제약 (C-TEC)

| # | 제약 |
| --- | --- |
| C-TEC-001·002 | Next.js App Router 단일 풀스택 · Server Actions / Route Handlers |
| C-TEC-003 | Prisma + Supabase (PostgreSQL · Storage) |
| C-TEC-004 | Tailwind + shadcn/ui |
| C-TEC-005·006 | Vercel AI SDK · Gemini (자체 서버 없이) |
| C-TEC-007 | Vercel 단일화 · **CI 없이 Git Push** |

### 파생 전제

- **A-T1** 함수 실행 시간 상한 → 장시간 추론·렌더를 서버에서 못 한다
- **A-T2** 요청 본문 크기 상한 → 4GB 업로드는 Storage 직접 전송
- **A-T3** 장시간 워커 불가 → 상주 데몬 없음. 재개는 사용자 재진입·webhook이 트리거
- **T1 확정** AI 추론만 외부 전용 API로 위임

### 조회·변경 배분

| 종류 | 방식 |
| --- | --- |
| 조회 | 🔴 **RSC 직접 조회** — Server Action으로 만들지 않는다 |
| 사용자가 일으키는 변경 | Server Action |
| 외부가 일으키는 사건 | Route Handler (webhook) |
| 정리 배치 | Vercel Cron |

---

## 게이트

| 게이트 | 판정 | 미달 시 |
| --- | --- | --- |
| **Gate A** | 🔴 **제외 후** 탐지율 ≥ 85% | **이후 개발 전면 중단** |
| **Gate B** | 기록 공간 수용성 | Phase 진행 중단 |

🔴 **순서를 바꿀 수 없다.** Gate A 실패 시 이후 단계에 착수하지 않는다.
🔴 **Gate A는 제외 후 값으로 판정한다** — 제외 전 값은 임계 τ와 독립이라 τ를 올려도 게이트가 저항하지 않는다. 제외 전은 진단용이다.

### 법무 게이트 4건 — 승인 전 해당 기능 배포 불가

| 게이트 | 차단 대상 |
| --- | --- |
| `MINOR_POLICY` | 🔴 **가입 플로우 전체** |
| `FACE_CONSENT` · `MINOR_SUBJECT` | 공개 발행 · 그룹 공개 |
| `MUSIC_LICENSE` | 음악 라이브러리 |

---

## 개발 작업 규칙

- **검증**: `npm run lint` · `typecheck` · `test` · `build` — 넷 다 통과해야 완료
- **오류를 억제로 넘기지 않는다** — `eslint-disable` 로 규칙을 끄는 대신 설계로 해소한다
- **테스트를 통과시키려고 테스트를 무력화하지 않는다**
- `main` 에 직접 push 하지 않는다. 브랜치 → PR
- 커밋 메시지 말미: `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

### Mock 우선

핵심 데이터가 외부 추론 API에서 오는데 **그 API가 아직 선정되지 않았다**(SP-1). `CT-007` Mock 픽스처가 이 사슬을 끊는다.

🔴 **실제 API로 교체할 때 호출부 코드 변경이 0건**이어야 한다. 그것이 Mock의 인수 기준이다.

---

## 문체

- 문서는 **평서체**(`~한다`)로 쓴다. 대화는 존댓말.
- 🔴 = 어기면 요구사항·제품 정의가 깨지는 것 · 🔺 = 미결·주의 · 🟢 = 지금 가능·안전
